{
    "$schema": "../../.jenkins/ci.schema.json",
    "dependencies": {
        "enabled": true,
        "command": "mvn dependency:go-offline"
    },
    "lint": {
        "enabled": false,
        "command": "mvn -B spotless:check checkstyle:check"
    },
    "test": {
        "enabled": false,
        "command": "mvn -B test"
    },
    "build": {
        "enabled": true,
        "command": "mvn -B package -DskipTests"
    },
    "image": {
        "enabled": false,
        "tagSeries": "be-jenkins-test",
        "repository": "docker/repository",
        "platform": {
            "single": false,
            "multi": [
                "linux/amd64",
                "linux/arm64"
            ]
        }
    }
}